{
  "name": "elysia-ravel",
  "version": "0.0.0",
  "private": true,
  "description": "A Laravel-inspired, Elysia-first application framework for Bun",
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
