import type { MiddlewareContext } from '@elyvel/core'
import { Controller } from '@elyvel/core'
{{modelImport}}
/**
 * {{class}} — a full 7-action resource controller (index/create/store/show/
 * edit/update/destroy), for rendering forms (e.g. Inertia pages). Wire it up
 * in a `routes/` file:
 *
 *   import { resource } from '@elyvel/core'
 *   import { {{class}} } from '../app/controllers/{{class}}'
 *   export default resource('/{{kebab}}', {{class}})
 */
export class {{class}} extends Controller {
  /** GET /{{kebab}} */
  async index(ctx: MiddlewareContext) {
    void ctx
    return []
  }

  /** GET /{{kebab}}/create */
  async create(ctx: MiddlewareContext) {
    void ctx
    return { form: 'create' }
  }

  /** POST /{{kebab}} */
  async store(ctx: MiddlewareContext) {
    return ctx.body
  }

  /** GET /{{kebab}}/:id */
  async show(ctx: MiddlewareContext) {
    return { id: ctx.params.id }
  }

  /** GET /{{kebab}}/:id/edit */
  async edit(ctx: MiddlewareContext) {
    return { form: 'edit', id: ctx.params.id }
  }

  /** PUT|PATCH /{{kebab}}/:id */
  async update(ctx: MiddlewareContext) {
    return { id: ctx.params.id, ...(ctx.body as object) }
  }

  /** DELETE /{{kebab}}/:id */
  async destroy(ctx: MiddlewareContext) {
    void ctx
    return { deleted: true }
  }
}

export default {{class}}
