import { Middleware, type MiddlewareContext } from '@elyvel/core'

/**
 * {{class}} — register it in `config/middleware.ts` under `aliases`, then apply
 * it to a route with `{ middleware: '{{alias}}' }` (or add it to a group).
 *
 * Return a response (e.g. `ctx.status(403, ...)`) to stop the request, or
 * return nothing to let it continue — like Laravel's `handle`.
 */
export class {{class}} extends Middleware {
  handle(ctx: MiddlewareContext) {
    // TODO: implement {{class}}
    void ctx
  }
}

export default {{class}}
