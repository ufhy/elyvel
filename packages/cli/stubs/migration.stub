import type { Migration } from '@elysia-ravel/eloquent'

export default {
  up: (schema) =>
    schema.create('{{table}}', (t) => {
      t.id()
      t.timestamps()
    }),
  down: (schema) => schema.dropIfExists('{{table}}'),
} satisfies Migration
