/**
 * Pesan pendaftaran milik @elyvel/auth (Bahasa Indonesia) — dimuat otomatis di
 * bawah namespace `auth::` (lihat `I18nServiceProvider`'s auto-discovery).
 * App bisa override key tertentu lewat `lang/vendor/auth/id/register.ts`.
 */
export default {
  // Ditampilkan pada field `email` saat email sudah dipakai akun lain.
  email_taken: 'Email ini sudah terdaftar.',
}
