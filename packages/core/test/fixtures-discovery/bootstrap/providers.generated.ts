// Fixture standing in for a real `elyvel package:discover` output.
import { DedupProvider } from '../providers/DedupProvider'
import { DiscoveredProvider } from '../providers/DiscoveredProvider'

export const discoveredProviders = [DiscoveredProvider, DedupProvider]
