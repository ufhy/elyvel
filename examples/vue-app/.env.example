APP_NAME="Vue App"
APP_ENV=local

# Secret for `encrypted` model casts (AES-256-GCM). Generate one with:
#   bun -e "console.log('base64:'+require('crypto').randomBytes(32).toString('base64'))"
APP_KEY=

PORT=3000

DB_CONNECTION=sqlite
